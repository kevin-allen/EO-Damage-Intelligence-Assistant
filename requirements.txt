# Web UI
streamlit>=1.36

# RAG: embeddings + vector store
sentence-transformers>=2.7
chromadb>=0.5
# Override chromadb's transitive pin to ancient otel exporter (incompatible with protobuf>=4).
opentelemetry-exporter-otlp-proto-grpc>=1.27

# LLM client (HTTP to Ollama)
requests>=2.31

# Data / imagery
pillow>=10
numpy>=1.26
pyyaml>=6

# CV utilities (torch and torchvision come from the pytorch/pytorch base image)
tqdm>=4.66
